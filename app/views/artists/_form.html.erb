<%= form_with(model: artist, local: true) do |form| %>
  <% if artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
      <% artist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.label :name %>
    <%= form.text_field :name, id: :artist_name, class: 'form-control'%>
  </div>

  <div class="field form-group">
    <%= form.label :genre_id %>
    <%= form.collection_select :genre_id, Genre.all, :id, :name, {prompt: 'Select a Genre'}, {class: 'form-control'}%>
    <!-- hard to understand collection_select. this is saying, refer to this stuff we're including in the form as genre_id. Get all instances from Genre class. Sort them by ID, but show me the name associated. -->
  </div>

  <div class="actions form-group">
    <%= form.submit class:"btn btn-submit btn-sm btn-warning" %>
  </div>
<% end %>
